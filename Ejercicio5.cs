using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio5 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*5.1
        a)True
        b)True
        c)True
        d)False(Si la exclamación aplica a ambos valores de la operación, en vez de a su resultado, entonces sí sería true)
        e)False
        f)True
        */

        /*5.2
         a)False
         b)True
         c)True
         d)True
         e)True
          f)False
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
